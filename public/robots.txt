# TEMPORARY robots.txt for the Railway staging / review deployment only.
#
# This file blocks ALL search-engine indexing while the site is parked on a
# Throwaway Railway domain. It must be REPLACED before the AWS production
# launch with proper SEO rules (the app also generates robots.txt via
# src/app/robots.ts, which is the file to update for final SEO).
#
# Deployment checklist:
#   1. Remove or update this file on the AWS production build.
#   2. Restore the real rule set in src/app/robots.ts before go-live.

User-agent: *
Disallow: /